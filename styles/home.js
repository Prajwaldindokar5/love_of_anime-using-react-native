import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  appTitle: {
    textAlign: "center",
    marginTop: 50,
    marginBottom:30,
    fontWeight: "bold",
    fontSize:15
  },
});

export default styles;
